import { Leaderboard } from '@components/leaderboard';

/**
 * Leaderboard App
 * 
 * A leaderboard feature that allows users to submit their scores
 * and view the top 20 players. Uses MongoDB for data persistence.
 */
const LeaderboardApp = () => {
  return <Leaderboard />;
};

export default LeaderboardApp;
